[project]
name = "clean-sweep"
version = "0.1.0"
description = "一键清空各类开发缓存及部分用户目录、应用配置的 Linux 命令行小工具"
readme = "README.md"
license = "GPL-3.0-or-later"
requires-python = ">=3.14"
dependencies = [
    "rich>=15.0.0",
    "textual>=8.2.7",
]
