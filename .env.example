# 复制本文件为 .env，填入你的 PyPI API token。
#   cp .env.example .env
# 然后编辑 .env 把 token 填进去。.env 已被 .gitignore 忽略，不会提交。
#
# token 在 https://pypi.org/manage/account/token/ 生成，以 pypi- 开头。
# scripts/publish.sh 会自动加载本目录下的 .env。

UV_PUBLISH_TOKEN=pypi-在这里填入你的token
